# ============================================================================
# core:designsystem — Material 3 Compose theme (minimal rules)
# ============================================================================

# Compose compiler handles its own optimization.
# No special keep rules needed for Compose UI code.
# Add rules here if custom reflection-based theming is introduced.
