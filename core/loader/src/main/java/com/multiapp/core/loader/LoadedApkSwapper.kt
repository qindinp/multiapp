package com.multiapp.core.loader

/**
 * 【最小 POC 骨架】LoadedApkSwapper 已被内联到 LoaderFactory
 *
 * 保留空对象以避免 StubBuilder 等模块编译报错。
 * POC 阶段不使用此类。
 */
object LoadedApkSwapper {
    // POC: 不再使用，逻辑内联到 LoaderFactory.swapClassLoader()
}
