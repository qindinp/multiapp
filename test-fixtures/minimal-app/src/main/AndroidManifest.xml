<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BROADCAST_STICKY" />

    <permission
        android:name="com.test.minimal.permission.PROBE_PROVIDER"
        android:protectionLevel="signature" />

    <application
        android:name=".MinimalApp"
        android:label="MinimalTest"
        android:theme="@android:style/Theme.Material.Light.DarkActionBar"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="MinimalTest"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SecondActivity"
            android:exported="false"
            android:label="MinimalSecond" />

        <service
            android:name=".ProbeService"
            android:exported="false" />

        <receiver
            android:name=".ProbeReceiver"
            android:exported="false" />

        <provider
            android:name=".ProbeProvider"
            android:authorities="com.test.minimal.probe"
            android:exported="false"
            android:permission="com.test.minimal.permission.PROBE_PROVIDER"
            android:grantUriPermissions="true" />

    </application>
</manifest>
